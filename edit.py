from pyspark.sql import DataFrame


def trainers_by_type(pokemon: DataFrame, trainers: DataFrame) -> DataFrame:
    """Fill in this function with code that returns a dataframe describing
    the number of each type of pokemon trained by each trainer
    """
    return pokemon
